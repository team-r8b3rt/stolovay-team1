// Общий JS для всех страниц.
// Этап 1: переключение роли происходит на сервере (POST /login, /logout),
// поэтому здесь пока только вспомогательная логика, если понадобится.
// TODO: на этапе 2 — обработка кликов по капелькам и т.п.

console.log("main.js загружен");
